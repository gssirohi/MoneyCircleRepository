<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    tools:context="company.greatapp.moneycircle.chooser.ChooserActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >

        <TextView
            android:id="@+id/tv_chooser_item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="Item Type"
            android:gravity="center"
            android:background="@color/app_basic"
            android:textAppearance="?android:attr/textAppearanceLarge" />

        <SearchView
            android:id="@+id/searchView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:searchIcon="@drawable/ic_search"
            android:iconifiedByDefault="false"
            android:queryHint="@string/action_search_hint"
            android:visibility="gone"/>
        <GridView
            android:id="@+id/gv_chooser_items"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="2dp"
            android:verticalSpacing="5dp"
            android:horizontalSpacing="2dp"
            android:numColumns="auto_fit"
            android:layout_gravity="center"
            android:gravity="center"
            android:scrollbars="none"

            />
        <ListView
            android:visibility="gone"
            android:id="@+id/lv_chooser_items"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal" />

    </LinearLayout>
</RelativeLayout>
